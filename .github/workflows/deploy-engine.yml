name: Deploy yQuant Engine

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy Engine Services
    runs-on: ubuntu-latest
    
    steps:
      - name: 🚀 Deploy via SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.ENGINE_HOST }}
          username: ${{ secrets.ENGINE_SSH_USER }}
          key: ${{ secrets.ENGINE_SSH_KEY }}
          port: ${{ secrets.ENGINE_SSH_PORT || 22 }}
          script: |
            cd ~/yquant
            git fetch --all
            git checkout ${{ github.ref_name }}
            git pull origin ${{ github.ref_name }}
            bash scripts/deploy-engine.sh
      
      - name: 🏥 Health Check
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.ENGINE_HOST }}
          username: ${{ secrets.ENGINE_SSH_USER }}
          key: ${{ secrets.ENGINE_SSH_KEY }}
          port: ${{ secrets.ENGINE_SSH_PORT || 22 }}
          script: |
            cd ~/yquant
            bash scripts/health-check-engine.sh
      
      - name: 📢 Notify Success
        if: success()
        run: |
          echo "✅ Engine Deployment successful!"
          echo "Ref: ${{ github.ref_name }}"
          echo "Commit: ${{ github.sha }}"
      
      - name: 📢 Notify Failure
        if: failure()
        run: |
          echo "❌ Engine Deployment failed!"
          echo "Check the logs above for details."
