services:
  # 1. Independent Redis for Demo
  demo-redis:
    image: redis:alpine
    ports:
      - "6380:6379"

  # 2. KIS Mock Server
  kis-mock:
    build: 
      context: ./kis-mock
    ports:
      - "9443:9443"

  # 3. Notifier (Optional, but good for demo)
  # demo-notifier:
  #   ...

  # Note: Other apps (BrokerGateway, OrderManager, Web) 
  # can be run locally pointing to these services
  # or added here for a full containerized demo.
